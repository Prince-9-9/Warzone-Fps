using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraRecoil : MonoBehaviour
{
    

    //public float recoilAmount = 0.1f; // adjust this value to change the amount of recoil
    //public float recoilRecovery = 0.1f; // adjust this value to change the recoil recovery speed
    //public float recoilMax = 0.5f; // adjust this value to change the maximum recoil
    //private Vector3 recoil;
    //private Vector3 recoilVelocity;

    //void Update()
    //{
    //    if (Input.GetButton("Fire1"))
    //    {
    //        recoil = new Vector3(-recoilAmount, 0, 0);
    //        recoil = Vector3.ClampMagnitude(recoil, recoilMax);
    //    }
    //    recoil = Vector3.SmoothDamp(recoil, Vector3.zero, ref recoilVelocity, recoilRecovery);
    //    transform.localRotation += recoil;
    //}
   
}
